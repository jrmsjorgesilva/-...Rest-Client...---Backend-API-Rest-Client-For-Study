require("dotenv").config();

async function pagSeguroCheckout(req, res) {
  const URL = `process.env.SERVER_URL/oauth2/application/process.env.CLIENT_ID`;
  // todo
}

module.exports = pagSeguroCheckout;
